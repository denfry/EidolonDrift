/**
 * Anomaly contract + categories (sound/world/client/entity subpackages). Each anomaly
 * implements {@code Anomaly} and self-registers into the Director's registry. Anomalies
 * are illusions by default — no real world mutation unless config-gated (invariant §1).
 * Empty until M1 (sound) / M3 (world) / M4 (client).
 */
package com.denfry.eidolondrift.anomaly;
