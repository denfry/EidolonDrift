/**
 * Layer 5 — The Observer (GDD §8). Not a boss: no loot, no HP bar. A phase machine
 * (ABSENT→…→FINAL) that reacts to being looked at and retreats from light &gt; 11 before
 * INVITED. Cannot break blocks (no grief, invariant §8). PERIPHERAL/SEEN prefer a
 * client-only rendered entity to avoid minimap traceability + desync. Empty until M2.
 */
package com.denfry.eidolondrift.observer;
