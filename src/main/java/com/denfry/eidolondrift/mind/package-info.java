/**
 * Layer 1 — State. Server-side {@code MindState} (invisible multi-dimensional fear
 * state, GDD §4), its full {@code Codec}, and {@code MindStateManager} decay/growth
 * tick. {@code distortion} is derived, never set directly. Communicate via symptoms
 * only — never expose raw numbers to the player. Empty until M1.
 */
package com.denfry.eidolondrift.mind;
