/**
 * Shared utilities — {@code PlayerLookUtil} (raycast look-detection), {@code ServerHelper}
 * (nearby-player checks), {@code WeightedRandom} (deterministic, RNG-injectable). The
 * {@code RL(String)} resource-location helper lives on {@code EidolonDrift}. Empty until needed.
 */
package com.denfry.eidolondrift.util;
